# Captures (local only)

Raw Wireshark / recorder outputs live here on the owner’s machine.

**They are gitignored** (size + privacy: session tokens, other players’ match traffic).

Do not commit `.pcap`, `.pcapng`, or large capture zips to GitHub.

Keep offline backups of this folder on encrypted/private storage if you need them for Stage 2 reverse-engineering.
