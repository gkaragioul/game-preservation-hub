# Reverse Engineering Notes

## Verified facts

- Workspace: `/path/to/spiral-warrior`.
- International player-facing artifact: `international_latest.xapk`.
- International package: `com.oversea.spinarena`.
- International version from XAPK manifest: `1.1.0.96`, version code `153`.
- Chinese reference artifact: `cn_9game.apk`.
- Engine: Cocos Creator / cocos2d-js, based on `assets/main.js`, `assets/src/*.jsc`, Cocos JSON asset layout, and `libcocos2djs.so`.
- Original downloaded archives and split APKs pass `sha256sum -c CHECKSUMS.sha256`.

## Important local evidence

- `triage_report.md` contains initial public-source triage.
- `research/endpoint_inventory.md` is generated by `tools/scan_endpoints.py`.
- `docs/ENGLISH_LOCALIZATION_NOTES.md` is generated by `tools/extract_cjk_strings.py`.

## Suspected startup flow [uncertain]

1. Android launches `com.oversea.spinarena`.
2. Native/Java SDK initializes Electronic Soul/DH/17m3 services.
3. Cocos runtime loads packaged assets and checks hot-update/resource manifests.
4. Client contacts login/config endpoints under `17m3.com` and/or Firebase.
5. Client displays login/server selection.
6. Client loads player profile and transitions to lobby.

This flow must be verified with `adb logcat` and network capture.

## Runtime evidence status

No emulator/device runtime evidence is currently verified in this workspace. See `docs/BLOCKERS.md`.

## Working principle

Implement only local/offline endpoints requested by the client. Do not rebuild a broad backend spec speculatively.
